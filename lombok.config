# tells lombok that this is the root directory and that it shouldn't
# search parent directories for more configuration files.
config.stopbubbling=true

# tells lombok to add lombok generated annotations to all generated methods
lombok.addLombokGeneratedAnnotation=true
